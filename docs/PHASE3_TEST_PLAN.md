# 第三阶段前端测试计划

## 🧪 测试环境检查

### 1. 环境要求
- [ ] Node.js >= 16.0.0
- [ ] npm >= 8.0.0
- [ ] 后端服务运行在 http://localhost:8000

### 2. 安装依赖
```bash
cd web
npm install
```

### 3. 启动开发服务器
```bash
npm run dev
```

---

## ✅ 功能测试清单

### 测试 1: 项目启动测试
**目标**: 验证项目能正常启动

```bash
cd C:/code/PycharmProjects/AIQuantification/web
npm install
npm run dev
```

**预期结果**:
- ✅ 依赖安装成功（无错误）
- ✅ Vite 服务器启动在 http://localhost:5173
- ✅ 控制台无报错

---

### 测试 2: 页面路由测试
**目标**: 验证所有页面路由正常

**测试步骤**:
1. 访问 http://localhost:5173
2. 应自动重定向到 /chat
3. 点击侧边栏各菜单项

**预期结果**:
- ✅ `/` → 自动跳转到 `/chat`
- ✅ `/chat` → AI 对话页面正常显示
- ✅ `/dashboard` → 市场仪表盘正常显示
- ✅ `/backtest` → 策略回测页面正常显示
- ✅ `/strategies` → 策略库页面正常显示

---

### 测试 3: AI 对话功能测试
**页面**: http://localhost:5173/chat

**测试用例 3.1: 基础对话**
```
输入: "你好"
预期: AI 返回问候语
```

**测试用例 3.2: 市场分析**
```
输入: "分析 AAPL 的技术面"
预期: 
- 显示工具调用（获取数据、计算指标）
- 返回技术分析结果
- Markdown 格式正确渲染
```

**测试用例 3.3: 流式响应**
```
输入: 任意问题
预期:
- 显示"AI 分析中..."加载提示
- 文字逐字显示（打字机效果）
- 完成后加载提示消失
```

**测试用例 3.4: 清空对话**
```
操作: 点击"清空对话"按钮
预期:
- 弹出确认对话框
- 确认后历史消息清空
- 显示空状态提示
```

**检查点**:
- [ ] 消息气泡样式正确（用户蓝色右侧，AI灰色左侧）
- [ ] Markdown 渲染正常（代码块、列表、加粗等）
- [ ] 工具调用可折叠展开
- [ ] 自动滚动到底部
- [ ] Ctrl+Enter 快捷发送

---

### 测试 4: 市场仪表盘测试
**页面**: http://localhost:5173/dashboard

**测试用例 4.1: 美股市场**
```
操作: 选择"美股"标签
预期: 显示美股行情卡片（AAPL, MSFT 等）
```

**测试用例 4.2: A股市场**
```
操作: 选择"A股"标签
预期: 显示A股行情卡片
```

**测试用例 4.3: 港股市场**
```
操作: 选择"港股"标签
预期: 显示港股行情卡片
```

**测试用例 4.4: 加密货币**
```
操作: 选择"加密货币"标签
预期: 显示加密货币行情（BTC, ETH 等）
```

**测试用例 4.5: 自动刷新**
```
操作: 等待 30 秒
预期: 数据自动刷新
```

**检查点**:
- [ ] 卡片显示：股票代码、价格、涨跌、成交量
- [ ] 涨跌颜色正确（绿涨红跌）
- [ ] 数字格式化正确（K/M/B）
- [ ] 卡片悬停效果
- [ ] 响应式布局（调整窗口大小）
- [ ] 刷新按钮可用

---

### 测试 5: 策略回测测试
**页面**: http://localhost:5173/backtest

**测试用例 5.1: 加载策略列表**
```
预期: 策略下拉框显示可用策略（sma_crossover, ema_crossover 等）
```

**测试用例 5.2: 表单验证**
```
操作: 不填写任何字段，点击"开始回测"
预期: 表单验证失败，显示错误提示
```

**测试用例 5.3: 运行回测**
```
步骤:
1. 选择策略: "sma_crossover"
2. 输入股票: "AAPL", "MSFT"
3. 选择日期: 2023-01-01 到 2024-01-01
4. 初始资金: 100000
5. 点击"开始回测"

预期:
- 显示加载状态
- 返回回测结果表格
- 显示统计汇总
```

**测试用例 5.4: 导出结果**
```
操作: 点击"导出结果"按钮
预期: 下载 CSV 文件
```

**检查点**:
- [ ] 策略列表正常加载
- [ ] 日期选择器限制未来日期
- [ ] 表格排序功能
- [ ] 夏普比率 Tag 颜色分级
- [ ] 胜率进度条显示
- [ ] 统计卡片正确计算平均值
- [ ] CSV 导出内容正确

---

### 测试 6: 策略库测试
**页面**: http://localhost:5173/strategies

**测试用例 6.1: 显示策略卡片**
```
预期: 显示所有可用策略的卡片
```

**测试用例 6.2: 策略详情**
```
操作: 查看各策略卡片
预期: 显示策略名称、描述、参数配置
```

**测试用例 6.3: 使用策略**
```
操作: 点击"使用此策略"按钮
预期: 跳转到回测页面，策略已预选
```

**检查点**:
- [ ] 卡片网格布局
- [ ] 卡片悬停效果
- [ ] 参数展示正确
- [ ] 跳转功能正常

---

### 测试 7: API 认证测试

**测试用例 7.1: 无 API Key（认证关闭）**
```
配置: 后端 security.require_auth = false
操作: 发送任意请求
预期: 正常工作
```

**测试用例 7.2: 配置 API Key**
```
操作:
1. 点击侧边栏"设置"按钮
2. 输入 API Key
3. 点击"保存"

预期: 显示"设置已保存"提示
```

**测试用例 7.3: 无效 API Key（认证开启）**
```
配置: 后端 security.require_auth = true
操作: 使用错误的 API Key 发送请求
预期: 显示"API Key 无效或已撤销"错误
```

**检查点**:
- [ ] API Key 保存到 LocalStorage
- [ ] 请求自动携带 X-API-Key header
- [ ] 401/403 错误正确处理

---

### 测试 8: 错误处理测试

**测试用例 8.1: 网络错误**
```
操作: 关闭后端服务，发送请求
预期: 显示"请求失败"错误提示
```

**测试用例 8.2: 429 限流**
```
操作: 快速连续发送多个请求
预期: 显示"请求过于频繁"提示
```

**测试用例 8.3: 500 服务器错误**
```
操作: 触发后端错误
预期: 显示"服务器错误"详细信息
```

---

### 测试 9: 响应式设计测试

**测试用例 9.1: 桌面端（>1200px）**
```
预期: 
- 侧边栏固定显示
- 卡片 4 列布局
```

**测试用例 9.2: 平板端（768-1200px）**
```
预期:
- 侧边栏收缩或隐藏
- 卡片 2-3 列布局
```

**测试用例 9.3: 手机端（<768px）**
```
预期:
- 侧边栏汉堡菜单
- 卡片 1 列布局
```

---

### 测试 10: 性能测试

**测试用例 10.1: 首屏加载**
```
操作: 刷新页面
预期: 
- FCP < 1s
- LCP < 2.5s
- 无明显卡顿
```

**测试用例 10.2: 路由切换**
```
操作: 快速切换各页面
预期: 切换流畅，无闪烁
```

**测试用例 10.3: 大量数据**
```
操作: 显示 100+ 条消息/卡片
预期: 滚动流畅，无内存泄漏
```

---

## 🐛 已知问题记录

| 问题 | 严重程度 | 复现步骤 | 状态 |
|------|---------|---------|------|
| | | | |

---

## 📊 测试结果汇总

### 浏览器兼容性
- [ ] Chrome (推荐)
- [ ] Edge
- [ ] Firefox
- [ ] Safari

### 功能完成度
- [ ] AI 对话: ___%
- [ ] 市场仪表盘: ___%
- [ ] 策略回测: ___%
- [ ] 策略库: ___%

### 性能指标
- FCP: ___ms
- LCP: ___ms
- TTI: ___ms

---

## 🚀 测试命令速查

```bash
# 安装依赖
cd web && npm install

# 开发模式
npm run dev

# 生产构建
npm run build

# 预览构建
npm run preview

# 类型检查
npm run type-check

# 检查端口占用
netstat -ano | findstr :5173
netstat -ano | findstr :8000
```

---

## 📝 测试报告模板

```markdown
## 测试日期: 2026-07-15
## 测试人员: [Your Name]
## 测试环境:
- Node.js: v__.__.__
- npm: v__.__.__
- 浏览器: Chrome v___
- 操作系统: Windows 11

## 测试结果:
✅ 通过: __/__ 项
❌ 失败: __/__ 项
⚠️  警告: __/__ 项

## 问题列表:
1. [问题描述]
2. [问题描述]

## 建议:
1. [改进建议]
2. [改进建议]
```
